{
  "name": "challenge",
  "version": "1.0.0",
  "description": "code challenge",
  "main": "src/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/",
    "start": "node src/index.js",
    "test": "node_modules/.bin/mocha test/**/*.test.js && node_modules/.bin/mocha  test/*.test.js",
    "standard-format": "npx standard --fix"
  },
  "author": "Freddy Chinchilla",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "standard": "^17.1.0"
  }
}
